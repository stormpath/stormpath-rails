require 'spec_helper'

describe Stormpath::Rails do
  it 'has a version number' do
    expect(Stormpath::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
