common:
  stormpath_url: <%%= ENV['STORMPATH_URL'] %>

development:
  root: https://api.stormpath.com/v1/directories/<root directory id>

test:
  root: https://api.stormpath.com/v1/directories/<root directory id>

production:
  root: https://api.stormpath.com/v1/directories/<root directory id>
